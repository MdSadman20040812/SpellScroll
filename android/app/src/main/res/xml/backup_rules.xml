<?xml version="1.0" encoding="utf-8"?>
<!-- WebView cookies and local storage hold the signed-in session, so they are
     backed up; cache directories are excluded by Android backup by default. -->
<full-backup-content>
    <include domain="database" path="." />
    <include domain="sharedpref" path="." />
</full-backup-content>
